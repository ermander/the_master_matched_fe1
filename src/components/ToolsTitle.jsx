import React from 'react'
import "../styles/_tools-title.scss"

export default function ToolsTitle({title}) {
    return (
        <div id="tools-title">
            <h2>{title}</h2>
        </div>
    )
}
